# zip-export

Demonstrates writing all batch output to one export file and compressing it at the end with `EXPORT-FILE-AS-ZIP=true`.

Run with `./gradlew corb -PcorbOptionsFile=zip-export/job.properties`.
