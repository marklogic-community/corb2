# batch-size-delimited

Demonstrates batching multiple URIs into each process-module invocation via `BATCH-SIZE` and using `BATCH-URI-DELIM` to tokenize the combined `$URI` value.

Run with `./gradlew corb -PcorbOptionsFile=batch-size-delimited/job.properties`.
