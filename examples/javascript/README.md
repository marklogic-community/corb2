# javascript

Demonstrates using JavaScript modules for init, URI selection, processing, and post-batch cleanup while exporting results to a file.

Run with `./gradlew corb -PcorbOptionsFile=javascript/job.properties`.
