# PostBatchUpdateFileTask-dedup

Demonstrates exporting results to a single file and then using `PostBatchUpdateFileTask` to sort and de-duplicate the final output with `EXPORT-FILE-SORT=ascending|distinct`.

Run with `./gradlew corb -PcorbOptionsFile=PostBatchUpdateFileTask-dedup/job.properties`.
