# inline-adhoc

Demonstrates an end-to-end CoRB job built entirely from inline adhoc modules, including URI generation, pre-batch header output, exported results, and post-batch cleanup.

Run with `./gradlew corb -PcorbOptionsFile=inline-adhoc/job.properties`.
