# test

Demonstrates a broader, exploratory example configuration with commented options for file loaders, disk queueing, command files, error handling, and export reporting.

Run with `./gradlew corb -PcorbOptionsFile=test/job.properties`.
