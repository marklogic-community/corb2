# feature-demo

Demonstrates several operational CoRB features together, including command-file control, disk queueing, metrics, job-server monitoring, and non-fatal error handling.

Run with `./gradlew corb -PcorbOptionsFile=feature-demo/job.properties`.
