# loader-zip

Demonstrates reading files from a ZIP archive with `FileUrisZipLoader`, passing each archive entry to the process module, and exporting a simple report of the loaded content.

Run with `./gradlew corb -PcorbOptionsFile=loader-zip/job.properties`.
