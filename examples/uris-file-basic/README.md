# uris-file-basic

Demonstrates driving a job from a plain client-side `URIS-FILE`, where each line is passed to the process module as a URI value.

Run with `./gradlew corb -PcorbOptionsFile=uris-file-basic/job.properties`.
