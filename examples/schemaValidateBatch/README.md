# schemaValidateBatch

Demonstrates validating XML in batch mode with `SchemaValidateBatchToFileTask` and wrapping validation errors in a single XML report using pre- and post-batch output content.

Run with `./gradlew corb -PcorbOptionsFile=schemaValidateBatch/job.properties`.
