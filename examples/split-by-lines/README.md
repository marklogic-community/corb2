# split-by-lines

Demonstrates splitting a single export into multiple output files based on `EXPORT-FILE-SPLIT-MAX-LINES`.

Run with `./gradlew corb -PcorbOptionsFile=split-by-lines/job.properties`.
