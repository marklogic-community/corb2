# schemaValidate

Demonstrates validating XML documents one at a time with `SchemaValidateToFileTask` and writing validation output to a file.

Run with `./gradlew corb -PcorbOptionsFile=schemaValidate/job.properties`.
