# loader-xml

Demonstrates loading nodes from a client-side XML file with `FileUrisStreamingXMLLoader` and processing each selected node as input to the process module.

Run with `./gradlew corb -PcorbOptionsFile=loader-xml/job.properties`.
